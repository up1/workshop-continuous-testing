apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: service-example
  rules:
  - http:
      paths:
      - path: /backend
        backend:
          service:
            name: backend
            port:
              number: 1323